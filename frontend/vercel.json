{
  "routes": [
    { "src": "/api/(.*)", "dest": "https://fxco-pilot.onrender.com/api/$1" },

    { "src": "/maintenance", "dest": "/maintenance.html" },
    { "src": "/(.*)", "dest": "/maintenance.html" },

    { "handle": "filesystem" }
  ]
}
